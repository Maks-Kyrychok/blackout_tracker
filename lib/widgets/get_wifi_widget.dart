import 'package:flutter/material.dart';

class GetWifiWidget extends StatelessWidget {
  const GetWifiWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
